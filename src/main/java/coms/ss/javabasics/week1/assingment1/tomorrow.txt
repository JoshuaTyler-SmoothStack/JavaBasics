#1
  a)
  (i) -> i % 2 != 0)

  b)
    static boolean isPrime(int n) 
    { 
        // Corner case 
        if (n <= 1) 
            return false; 
  
        // Check from 2 to n-1 
        for (int i = 2; i < n; i++) 
            if (n % i == 0) 
                return false; 
  
        return true; 
    } 

  c)
  .reverse() = normal

#2
  % 10

#3
  map((i) -> i * 2)

#4
  map((str) -> str.replace("x", ""))

#5
  int[][] identicalValues
  for(i++)
  for(ii++)
  